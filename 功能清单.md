# DeepSeek Harness 桌面端 · 功能清单（最小版）

- **来源**：基于开源项目 [xiincs/deepseek-harness-desktop](https://github.com/xiincs/deepseek-harness-desktop) v1.4.0（MIT 许可）自建定制
- **技术栈**：Tauri 2（Rust + 系统 WebView2），壳界面为无框架前端（`ui/`），承载内容为官方 DeepSeek Harness Web 界面（`@deepseek-ai/dsh`，默认 `0.1.0-rc.6`）
- **平台**：Windows x64（NSIS 每用户安装，免管理员权限）
- **定位**：最小托盘常驻启动器——双击桌面图标唤起 `dsh`，关窗隐藏到托盘后台常驻，无需命令行。

> 本版本为「阉割版」：已移除内置 Node 运行时（改用系统 Node）、右侧原生 dock（文件面板 / 终端 / 插件市场）及自动更新通道。保留项见下。

---

## 一、核心功能（保留）

| # | 功能 | 说明 |
|---|---|---|
| 1 | 一键启动 | 双击桌面图标 → 定位系统 Node（`DSH_DESKTOP_NODE` → PATH）→ 拉起 `dsh web` → 从 stdout 解析就绪 URL → 窗口加载官方界面 |
| 2 | dsh 运行时托管 | 优先 `DSH_DESKTOP_DSH_BIN`/已装运行时目录，否则首次使用时经 npm 自动安装到 `%LOCALAPPDATA%\dev.dsh.desktop\runtime`（有进度日志） |
| 3 | 最大化窗口 | 启动即最大化（可还原 1280×820，最小 960×640），无边框外观 + 自定义窗口控制按钮 |
| 4 | 单实例 | 重复点击图标只聚焦已有窗口；支持传目录参数切换工作区（配合 Explorer 右键"用 DeepSeek Harness 打开"） |
| 5 | 关窗 = 后台常驻 | 点 ✕ 仅隐藏窗口，dsh 服务继续运行；首次关闭弹通知"已转入后台" |
| 6 | 托盘常驻入口 | 系统托盘；左键显示窗口，右键菜单：显示窗口 / 在浏览器中打开 / 重启服务 / 打开数据目录 / 开机自启（默认关）/ 退出 |
| 7 | 显式退出才终止 | 托盘"退出"→ `taskkill /T /F` 清理 dsh 整棵进程树，不残留孤儿进程 |
| 8 | 服务接管与端口容错 | 先探测 `127.0.0.1:3080`：已有 harness 在跑则直接接管复用；端口被占则改用系统分配端口 |
| 9 | 崩溃自愈 | dsh 异常退出后 1 秒自动重启（60 秒窗口限流一次），重启后窗口刷新到新端口 |
| 10 | 启动兜底 | 失败/超时显示中文错误页 + 重试/重启服务/查看日志/在浏览器中打开 |
| 11 | PATH 自动重建 | 从注册表（机器级+用户级）重建 PATH 并展开 `%VAR%` 占位符，修复 GUI 进程 PATH 过时问题 |
| 12 | 全局快捷键 | 任意位置 `Ctrl+Alt+D` 唤起窗口（注册失败不影响启动） |
| 13 | 数据互通 | 与网页版/CLI 共用 `%USERPROFILE%\.dsh`，会话、配置完全一致 |
| 14 | `~/.dsh` 自愈 | dsh 引导报错提示的陈旧 profiles/node_modules 目录自动清理重试（窄条件、有界） |

## 二、WebView 体验与安全边界（保留）

| # | 功能 | 说明 |
|---|---|---|
| 15 | iframe 隔离 | harness 页面永不授予 Tauri IPC（`dangerousRemoteDomainIpcAccess` 永不开启） |
| 16 | 精简右键菜单 | 移除 back/forward/reload/inspectElement，保留复制/粘贴等常用项 |
| 17 | 缩放控制 | 禁用 Ctrl+滚轮 / Ctrl+± 页面缩放 |
| 18 | DevTools 管理 | 发布版禁用 F12/DevTools，调试版保留 |
| 19 | 外链跳系统浏览器 | 非 127.0.0.1/localhost 的 http(s) 链接一律转发系统默认浏览器 |
| 20 | 安全默认值 | contextIsolation 开启 + nodeIntegration 关闭 |

## 三、配置与环境变量（诊断入口）

| 变量 | 作用 | 默认 |
|---|---|---|
| `DSH_DESKTOP_NODE` | 指定 node.exe 绝对路径 | PATH 上的 node |
| `DSH_DESKTOP_DSH_BIN` | 指定 dsh `lib/bin.js` 绝对路径 | 托管运行时 |
| `DSH_DESKTOP_RUNTIME_DIR` | 托管 dsh 运行时目录（指向已有 node_modules 可跳过首装） | `%LOCALAPPDATA%\dev.dsh.desktop\runtime` |
| `DSH_DESKTOP_DSH_VERSION` | 托管运行时 npm 版本 | `0.1.0-rc.6` |
| `DSH_DESKTOP_PORT` | 默认绑定端口 | `3080` |
| `DSH_DESKTOP_CWD` | dsh 服务工作目录 | 用户主目录（或启动参数传入的目录） |
| `DSH_HOME` | harness 数据根目录 | `~/.dsh` |

应用自身日志：`%LOCALAPPDATA%\dev.dsh.desktop\logs\desktop.log`

## 四、打包与分发

| # | 功能 | 说明 |
|---|---|---|
| 21 | NSIS 安装包 | 每用户安装（免管理员），中文界面，含开始菜单快捷方式与卸载程序 |
| 22 | Explorer 右键集成 | 文件夹及文件夹空白处右键 →"用 DeepSeek Harness 打开"（HKCU，卸载时清理） |
| 23 | 构建命令 | `npm install` → `npm run build`；产物 `src-tauri\target\release\bundle\nsis\DeepSeek Harness_<版本>_x64-setup.exe` |

## 五、本阉割版移除项

- ❌ **内置 Node 运行时**（`resources/runtime`，node.exe + 完整依赖树，约 340 MB）——改为依赖系统 Node，安装包体积大幅缩小
- ❌ **右侧原生 dock 全部**：文件面板（目录树/Git 状态/预览编辑）、内置终端（ConPTY）、插件市场（社区目录/安装/pnpm 检测）及文件提及桥接
- ❌ **自动更新通道**（updater 插件与 latest.json 早已停用，本次连残留 UI 一并清除）
- ❌ 对应构建脚本 `fetch-node.mjs`/`prepare-runtime.mjs`/`build-latest-json.mjs`、`ui/vendor/`（CodeMirror/xterm）

## 六、已知限制

- 仅打包 Windows x64（NSIS）；macOS/Linux 分支代码存在但未验证
- 依赖系统 Node.js（未安装时错误页会给出提示与安装链接）
- 无自动更新；升级需重新构建安装包覆盖安装
